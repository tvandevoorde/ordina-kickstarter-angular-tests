import { NavbarToggleDirective } from './navbar-toggle.directive';

describe('NavbarToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarToggleDirective(null);
    expect(directive).toBeTruthy();
  });
});
